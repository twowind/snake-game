{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/main",
            "preLaunchTask": "Build C++ project",
            "cwd": "${workspaceFolder}",
        }
    ]
}